# README: YooKassa Tax Bot для НПД

Telegram-бот для автоматической обработки реестров YooKassa и подготовки данных для внесения в «Мой налог» (самозанятые).

## 🎯 Возможности

### Основные функции:
- ✅ **Автоматическая проверка почты** (1 раз в сутки) и скачивание CSV-реестров
- ✅ **Парсинг реестров** без pandas (только стандартный csv)
- ✅ **Подготовка данных для НПД**: готовая строка для ручного внесения в «Мой налог»
- ✅ **Дедупликация**: не обрабатывает одно письмо дважды
- ✅ **Безопасность**: только разрешённые admin ID могут использовать бота
- ✅ **Long polling**: не требует настройки webhook/nginx

### Новые фичи:
- 📊 **Красивый вывод** с inline-кнопками (Показать детали / Скачать CSV / Удалить)
- 📈 **Статистика НПД**: доходы за месяц, год, лимит 2.4 млн
- 📋 **История реестров**: последние 15 записей
- ⚙️ **Настройки через Telegram**: включить/выключить уведомления, изменить описание
- 🗑 **Удаление сообщений**: кнопка "Удалить" на каждом уведомлении
- 🚫 **Игнорирование возвратов**: файлы refunds не обрабатываются

---

## 🚀 Быстрый старт

### 1. Создание Telegram-бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям (имя бота, username)
4. Скопируйте **токен** (например: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)
5. Узнайте свой Telegram ID:
   - Откройте [@userinfobot](https://t.me/userinfobot)
   - Скопируйте ваш **ID** (например: `123456789`)

### 2. Настройка IMAP

Получите данные для доступа к почте:
- **Gmail**: включите IMAP в настройках, создайте [App Password](https://myaccount.google.com/apppasswords)
- **Yandex/Mail.ru**: включите IMAP в настройках, используйте пароль от почты

Нужны:
- `IMAP_HOST` (например: `imap.gmail.com`)
- `IMAP_PORT` (обычно `993`)
- `IMAP_USER` (ваш email)
- `IMAP_PASSWORD` (пароль или app-password)

### 3. Установка на сервер

```bash
# Создайте директорию проекта
mkdir -p /opt/yookassa-tax-bot
cd /opt/yookassa-tax-bot

# Клонируйте репозиторий
git clone https://github.com/YOUR_USERNAME/yookassa-tax-bot.git .

# Создайте .env из .env.example
cp .env.example .env
nano .env  # Заполните все переменные

# Запустите
docker compose up -d

# Проверьте логи
docker compose logs -f
```

### 4. Использование

В Telegram напишите боту:

**Основные команды:**
- `/start` — приветствие и список команд
- `/run` — вручную запустить проверку почты
- `/status` — статистика обработки (письма, файлы)

**Статистика и история:**
- `/stats` — доходы НПД (месяц, год, лимит)
- `/history` — последние 15 реестров

**Настройки:**
- `/settings` — настройки бота (уведомления, описание)

Бот автоматически проверяет почту каждый день в указанное время (`DAILY_HOUR:DAILY_MINUTE` по `TIMEZONE`).

---

## 📊 Пример работы

### Уведомление о реестре:

```
📊 Реестр от 2026-01-15

💰 Доход: 1,245.50 RUB
📦 Платежей: 12
💸 Комиссия: 31.14 RUB (справочно)

Для «Мой налог»:
2026-01-15 — 1245.50 RUB — Доступ к IT-сервису

[📊 Показать детали] [📄 Скачать CSV] [🗑 Удалить]
```

### Детализация платежей:

При клике "📊 Показать детали":
```
📋 Детализация платежей (12 шт.)

1️⃣ 10:23 — 99.00 RUB (Подписка на месяц)
2️⃣ 11:45 — 149.00 RUB (Подписка на 3 месяца)
3️⃣ 14:20 — 249.00 RUB (Годовая подписка)
...
```

### Статистика НПД:

```
📊 Статистика доходов НПД

За текущий месяц (январь 2026):
💰 Доход: 45,230.00 RUB
💸 Комиссия: 1,130.50 RUB
📦 Платежей: 234
📅 Дней с доходом: 15/31

За 2026 год:
💰 Доход: 156,890.00 RUB
💸 Комиссия: 3,922.25 RUB
📦 Платежей: 892

Лимит НПД 2026:
🟢 156,890.00 / 2,400,000 RUB (6.5%)
```

---

## ⚙️ Настройки (.env)

```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=ваш_токен
ADMIN_IDS=ваш_telegram_id

# IMAP Configuration
IMAP_HOST=imap.mail.ru
IMAP_PORT=993
IMAP_USER=your-email@mail.ru
IMAP_PASSWORD=ваш_пароль

# Email Filters
EMAIL_FROM_FILTER=reports@yoomoney.ru
EMAIL_SUBJECT_FILTER=Реестр
DAYS_TO_CHECK=7
ALLOWED_EXTENSIONS=.csv

# Scheduler
TIMEZONE=Europe/Moscow
DAILY_HOUR=10
DAILY_MINUTE=0

# Tax Description
TAX_DESCRIPTION=Доступ к IT-сервису (подписка)

# Notifications
NOTIFY_EMPTY_REGISTRIES=true

# НПД Limit
NPD_YEARLY_LIMIT=2400000

# Logging
LOG_LEVEL=INFO
```

---

## 🔧 Обновление бота

Когда вы внесёте изменения в код:

### На локальном компьютере:

```bash
git add .
git commit -m "Описание изменений"
git push
```

### На сервере:

```bash
cd /opt/yookassa-tax-bot
sudo git pull
sudo docker compose down
sudo docker compose up -d --build
```

---

## 📦 Структура данных

Бот хранит данные в SQLite (`/app/data/bot.db`):
- **processed_files** — обработанные письма (дедупликация)
- **registries** — история реестров
- **payments** — детализация платежей
- **stats** — статистика обработки
- **settings** — настройки пользователя

Временные CSV файлы: `/app/data/temp/`

---

## 💡 Полезные команды Docker

```bash
# Просмотр логов
docker compose logs -f

# Перезапуск бота
docker compose restart

# Остановка
docker compose down

# Очистка и пересборка
docker compose down
docker compose up -d --build --force-recreate

# Проверка использования ресурсов
docker stats yookassa-tax-bot
```

---

## 🛡 Безопасность

✅ **Что загружается на GitHub:**
- Весь код проекта
- `.env.example` (шаблон без секретов)

❌ **Что НЕ загружается (защищено .gitignore):**
- `.env` (ваши токены и пароли)
- `data/` (база данных и временные файлы)

**Важно:** Никогда не коммитьте `.env` файл!

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `docker compose logs -f`
2. Убедитесь, что `.env` правильно заполнен
3. Проверьте настройки IMAP (возможно нужен App Password)

---

## 🎉 Готово!

Теперь ваш бот:
- ✅ Автоматически обрабатывает реестры YooKassa
- ✅ Готовит данные для «Мой налог» одной строкой
- ✅ Показывает статистику и историю
- ✅ Управляется через Telegram
- ✅ Не отправляет лишние файлы (только по запросу)
- ✅ Красиво форматирует сообщения
- ✅ Отслеживает лимит НПД

**Успехов с самозанятостью!** 💪